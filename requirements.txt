# ── Core (preprocess / calibrate / event I/O) ──────────────────────
opencv-python>=4.8.0
numpy>=1.24.0,<2        # DVS-Voltmeter pins numpy<2 for its older ABI; see setup_sim.sh.
                         # Keep the project on one NumPy major version to avoid cross-env ABI drift.
tifffile>=2023.1.0
PyYAML>=6.0
h5py>=3.8.0

# ── Label transfer / dataset / visualize ────────────────────────────
matplotlib>=3.7.0        # visualize_geometry_error.py radial-error plots

# ── Train / eval ─────────────────────────────────────────────────────
ultralytics>=8.1.0       # train_event_yolo.py / eval_event_yolo.py

# ── Optional simulator-search dependency ───────────────────────────
# Optuna is deliberately not a base dependency. Install `optuna>=3.5.0` only
# for the explicitly experimental `calibrate_simulator.py --optuna` path, in
# the host environment that runs that script (not the v2e/DVS-Voltmeter envs).

# ── Do not install these here; follow each repository's setup ──────
# v2e             -> setup_sim.sh (separate conda env, Python 3.10)
# DVS-Voltmeter   -> setup_sim.sh (separate conda env, Python 3.9, old OpenCV pin)
# SAM3            -> facebookresearch/sam3; see test_sam3.py and sam3_export_tracks.py
# Arena SDK       -> build_linux.sh (C++ binding, not a pip package)

# ── Legacy-only dependency note ─────────────────────────────────────
# metavision-sdk-base  -> used only by record_evs.py/read_evt3.py. The current
#                         acquisition path is evs_recorder.cpp; keep the legacy
#                         files isolated so they cannot be invoked accidentally.
